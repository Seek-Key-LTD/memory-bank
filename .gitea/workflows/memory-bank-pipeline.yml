name: Memory Bank Cognitive Pipeline

on:
  push:
    branches: [ "*" ]

jobs:
  run-demo-pipeline:
    runs-on: ash3c
    steps:
      - name: Execute Pipeline Demonstration (Access -> Aggregation -> Core)
        run: python3 /opt/mem-ops/demo_memory_bank.py
